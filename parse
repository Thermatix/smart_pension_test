#!/usr/bin/env ruby
# frozen_string_literal: true

require 'lib/parser'

case ARGV.first
when '-h', '--help'
  puts <<~TEXT
    usage: parse file_name.log
           parse [-h | --help]: display help
  TEXT
else
  process_logs(ARGB.first)
end
